name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: uname
      run: uname
    - name: Build mod
      run: sh ./tools/build.sh
