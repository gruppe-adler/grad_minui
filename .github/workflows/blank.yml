name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: DerZade/gh-action-build-hemtt-mod@master
    - uses: actions/checkout@v1
    - name: Build mod
      run: ./tools/hemtt build --release --force --opts all
