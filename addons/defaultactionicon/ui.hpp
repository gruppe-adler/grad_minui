class CfgInGameUI {
  class DefaultAction {
    size = "((((safezoneW / safezoneH) min 1.2) / 1.2) / 25) * (['defaultactionicon_size'] call (uinamespace getvariable 'grad_minui_fnc_setting'))";
  };
};
