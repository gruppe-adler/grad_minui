{
    "name": "grad_minui",
    "prefix": "grad_minui",
    "author": "DerZade",
    "files": [
        "mod.cpp",
        "Adler.paa",
        "LICENSE",
        "README.md"
    ],
    "keyname": "{{prefix}}-{{version}}",
    "signame": "{{prefix}}-{{version}}",
    "releasebuild": [
        "!postBuild"
    ],
    "script": {
        "postBuild": {
            "show_output": true,
            "steps": [
                "bash ./tools/postBuild.sh {{name}} {{prefix}} {{version}}"
            ]
        }
    }
}